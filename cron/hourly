#!/bin/bash
set -exu

SCRIPT=$(readlink -f $0)
SCRIPTDIR="${SCRIPT%/*}"
BASEDIR="${SCRIPTDIR%/*}"

cd $BASEDIR

update()
{
	git pull

	./install.sh
}

update 2>&1 | Mail -s "Automaton update $HOSTNAME" scott@griepentrog.com
